# Mekanos

`model-and-walk.fbx` is the unchanged user upload previously named
`public/assets/Walking kopio.fbx`. It includes the skin, embedded texture,
skeleton and dedicated walk clip. Runtime ownership: `MechanosRig.ts` and the
`MECHANOS_WALK` semantic animation entry. Do not retarget its proportions onto
the player skeleton.
